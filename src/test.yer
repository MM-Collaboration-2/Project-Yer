Func(countdown){
	yell($argv0);
	If($argv0 > 0){
		countdown($argv0 - 1);
	}
	return;
}
Func(add){
  return $argv0 + $argv1 + $argv2 + $argv3;
  
}
Func(sum_square){
  c = add($argv0, $argv1);
  return c * c;
}

countdown(4);
s = sum_square(4, 4);
yell(s);
